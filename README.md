# YouTube Study Application

แอปพลิเคชันจัดการความรู้ส่วนบุคคลสำหรับเนื้อหา YouTube ที่ช่วยให้คุณสามารถรวบรวม จัดหมวดหมู่ และโต้ตอบกับเนื้อหาวิดีโอผ่านการถอดเสียง สรุปเนื้อหา และความสามารถในการแชทด้วย AI

## ✨ คุณสมบัติหลัก

- 📹 **การจัดเก็บวิดีโอ**: เพิ่มวิดีโอ YouTube ผ่าน URL หรือ Video ID พร้อมการนำเข้าแบบกลุ่มจาก Playlist
- 🏷️ **การจัดหมวดหมู่อัจฉริยะ**: สร้างและจัดการหมวดหมู่ พร้อมระบบแท็กหลายแท็ก
- 📝 **การถอดเสียงและสรุป**: ถอดเสียงอัตโนมัติและสร้างสรุปหลายระดับ (สรุปสั้น, สรุปละเอียด, จุดสำคัญ)
- 🔍 **ค้นหาและกรอง**: ค้นหาวิดีโอตามชื่อ ผู้สร้าง หมวดหมู่ และสถานะการรับชม
- 📊 **แดชบอร์ด**: ภาพรวมสถิติและวิดีโอล่าสุด
- 🎨 **UI สวยงาม**: ออกแบบด้วย Next.js และ TailwindCSS

## 🚀 การติดตั้งและใช้งาน

### ข้อกำหนดเบื้องต้น

- Docker และ Docker Compose
- (ไม่บังคับ) YouTube Data API Key
- (ไม่บังคับ) OpenAI API Key หรือ Gemini API Key

### วิธีการติดตั้ง

1. **Clone Repository**

```bash
git clone <repository-url>
cd youtube-study-app
```

2. **ตั้งค่า Environment Variables**

สร้างไฟล์ `.env` จาก `.env.example`:

```bash
cp .env.example .env
```

แก้ไขไฟล์ `.env` และใส่ API Keys ของคุณ (ไม่บังคับ):

```env
# YouTube Data API (ไม่บังคับ - สำหรับข้อมูลเพิ่มเติม)
YOUTUBE_API_KEY=your_youtube_api_key

# LLM Provider (openai, gemini, หรือ local)
LLM_PROVIDER=openai

# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key

# หรือ Gemini API Key
GEMINI_API_KEY=your_gemini_api_key
```

3. **สร้างและรัน Docker Container**

```bash
docker-compose up --build
```

รอจนกว่า container จะเริ่มต้นเสร็จ (ประมาณ 30-60 วินาที)

4. **เข้าใช้งานแอปพลิเคชัน**

เปิดเบราว์เซอร์และไปที่:

```
http://localhost:3000
```

## 📖 วิธีใช้งาน

### การเพิ่มวิดีโอ

1. คลิกปุ่ม "Add Video" บนแดชบอร์ดหรือหน้า Videos
2. วาง YouTube URL หรือ Video ID
3. เลือกหมวดหมู่ (ไม่บังคับ)
4. เพิ่มแท็ก (ไม่บังคับ)
5. คลิก "Add Video"

แอปจะทำการ:
- ดึงข้อมูลวิดีโอ (ชื่อ, ผู้สร้าง, ระยะเวลา, ภาพขนาดย่อ)
- ถอดเสียงจาก YouTube captions
- สร้างสรุปด้วย AI (สรุปสั้น, สรุปละเอียด, จุดสำคัญ)
- จัดทำดัชนีสำหรับ Knowledge Graph

### การจัดการวิดีโอ

- **ดูวิดีโอ**: คลิกที่การ์ดวิดีโอเพื่อดูรายละเอียด
- **เปลี่ยนสถานะ**: เลือกสถานะ Unwatched, Watching, หรือ Watched
- **ค้นหา**: ใช้แถบค้นหาเพื่อหาวิดีโอตามชื่อหรือผู้สร้าง
- **กรอง**: กรองตามหมวดหมู่หรือสถานะการรับชม
- **มุมมอง**: สลับระหว่างมุมมองแบบ Grid และ List

### การสร้างหมวดหมู่

1. ไปที่หน้า Dashboard
2. ดูหมวดหมู่ที่มีอยู่ในส่วน Categories
3. สร้างหมวดหมู่ใหม่ผ่าน API หรือเพิ่มขณะเพิ่มวิดีโอ

## 🏗️ สถาปัตยกรรม

### Backend (Node.js + Express)

```
backend/
├── src/
│   ├── server.js           # Express server
│   ├── routes/             # API routes
│   │   ├── videos.routes.js
│   │   ├── categories.routes.js
│   │   ├── graph.routes.js
│   │   └── chat.routes.js
│   ├── services/           # Business logic
│   │   ├── youtube.service.js
│   │   ├── transcription.service.js
│   │   ├── llm.service.js
│   │   └── rag.service.js
│   └── prisma/
│       └── schema.prisma   # Database schema
```

### Frontend (Next.js + React)

```
frontend/
├── app/
│   ├── page.tsx            # Dashboard
│   ├── videos/
│   │   ├── page.tsx        # Videos list
│   │   └── [id]/
│   │       └── page.tsx    # Video detail
│   └── layout.tsx
├── components/             # Reusable components
└── lib/
    └── api.ts             # API client
```

### Database (SQLite + Prisma)

- **Videos**: ข้อมูลวิดีโอ, การถอดเสียง, สรุป
- **Categories**: หมวดหมู่วิดีโอ
- **KnowledgeGraph**: ความสัมพันธ์ระหว่างวิดีโอ
- **ChatSessions**: ประวัติการแชท

## 🔧 API Endpoints

### Videos

- `GET /api/videos` - รายการวิดีโอทั้งหมด
- `GET /api/videos/:id` - รายละเอียดวิดีโอ
- `POST /api/videos` - เพิ่มวิดีโอใหม่
- `PUT /api/videos/:id` - อัปเดตวิดีโอ
- `DELETE /api/videos/:id` - ลบวิดีโอ
- `POST /api/videos/:id/process` - ประมวลผลวิดีโอ (ถอดเสียง + สรุป)
- `POST /api/videos/batch` - นำเข้าจาก playlist

### Categories

- `GET /api/categories` - รายการหมวดหมู่
- `GET /api/categories/:id` - รายละเอียดหมวดหมู่
- `POST /api/categories` - สร้างหมวดหมู่
- `PUT /api/categories/:id` - อัปเดตหมวดหมู่
- `DELETE /api/categories/:id` - ลบหมวดหมู่

### Knowledge Graph

- `GET /api/graph` - ข้อมูล Knowledge Graph
- `GET /api/graph/relationships/:id` - ความสัมพันธ์ของวิดีโอ
- `POST /api/graph/rebuild` - สร้าง graph ใหม่

### Chat

- `POST /api/chat` - สร้าง chat session
- `GET /api/chat/:id` - ดู chat session
- `POST /api/chat/:id/message` - ส่งข้อความ
- `DELETE /api/chat/:id` - ลบ chat session

## 🛠️ การพัฒนา

### รัน Backend แยก

```bash
cd backend
npm install
npm run dev
```

### รัน Frontend แยก

```bash
cd frontend
npm install
npm run dev
```

### Database Migrations

```bash
cd backend
npx prisma migrate dev
npx prisma studio  # เปิด Prisma Studio
```

## 📝 หมายเหตุ

### การใช้งานโดยไม่มี API Keys

แอปสามารถทำงานได้โดยไม่ต้องใช้ API Keys:

- **YouTube**: ใช้ `ytdl-core` สำหรับดึงข้อมูลพื้นฐาน
- **Transcription**: ใช้ `youtube-transcript-api` (Python)
- **LLM**: ต้องมี API Key หรือใช้ Local LLM (Ollama)

### ข้อจำกัด

- การถอดเสียงใช้ได้เฉพาะวิดีโอที่มี captions
- การสรุปต้องการ LLM API Key
- ไม่มี YouTube Data API Key = ข้อมูลวิดีโอจำกัด

## 🐛 การแก้ไขปัญหา

### Container ไม่เริ่มต้น

```bash
docker-compose down
docker-compose up --build
```

### Database Error

```bash
docker-compose down -v  # ลบ volumes
docker-compose up --build
```

### Port ถูกใช้งานอยู่

แก้ไข `docker-compose.yml` เปลี่ยน port:

```yaml
ports:
  - "3001:3000"  # เปลี่ยนจาก 3000 เป็น 3001
```

## 📚 เทคโนโลยีที่ใช้

- **Backend**: Node.js, Express, Prisma, SQLite
- **Frontend**: Next.js 14, React, TailwindCSS, TypeScript
- **Video Processing**: ytdl-core, youtube-transcript-api
- **AI/ML**: OpenAI API, Google Gemini API, LightRAG
- **Deployment**: Docker, Docker Compose

## 🎯 แผนการพัฒนาต่อ (Phase 2-4)

- [ ] Knowledge Graph Visualization (D3.js/Cytoscape.js)
- [ ] Chat Interface with RAG
- [ ] Browser Extension
- [ ] Mobile Responsive Design
- [ ] Export to Obsidian/Notion
- [ ] Analytics Dashboard

## 📄 License

MIT License

## 🤝 การมีส่วนร่วม

ยินดีรับ Pull Requests และ Issues!

---

**สร้างด้วย ❤️ สำหรับผู้ที่รักการเรียนรู้จาก YouTube**
